# Theme tokens (future mobile)

Tokens de diseno para React Native, alineados con el sistema visual web.

Sugerido:

- `colors.ts`
- `spacing.ts`
- `typography.ts`
- `radius.ts`

Objetivo: mantener paridad visual sin compartir componentes DOM.
