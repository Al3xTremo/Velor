# App layer (future Expo)

Esta carpeta se reserva para las pantallas y navegacion de React Native.

Propuesta futura:

- `navigation/` para stack/tab navigators
- `screens/` por feature (`auth`, `dashboard`, `transactions`, `budgets`)
- composicion de providers de app movil
